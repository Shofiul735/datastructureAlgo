/*
    Max Heap follows only one rules:
        Childs must be smaller than the parent.
        Or, The parent must larger than childs.
    There is no rules like left has to be smaller or right has to be larger than the parent
*/